/**
 * Sequelize models. Import and associate here, then export for use in repositories.
 */

import { sequelize } from '../index.js';

// Example: define and export models
// import { User } from './user.js';
// export { User };

export { sequelize };
